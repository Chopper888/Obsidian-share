#Have this script directly into your espanso/user.
# You can change and add some triggers too !
# To use it, it's simply the trigger (: or /) before all aliases for the admonition. 
# For example : :faq or /faq will type the admonition question. 
name: "Admonition"
parent: "Obsidian"

matches:
#Title
  - triggers: [":note", ":seealso"]
    replace: |
     ```ad-note
     title: $|$
     ```
  - triggers: [":tldr",":summ", ":summary"]
    replace: |
     ```ad-abstract
     title: $|$
     ```
  - triggers: [":info",":todo"]
    replace: |
     ```ad-info
     title: $|$
     ```
  - triggers: [":tip", ":hint", ":important", ":def"]
    replace: |
     ```ad-tip
     title:$|$
     ```
  - triggers: [":success", ":suc", ":check",":done"]
    replace: |
     ```ad-success
     title: $|$
     ```
  - triggers: [":question", ":help",":faq", ":q"]
    replace: |
     ```ad-question
     title: $|$
     ```
  - triggers: [":warn", ":warning", ":caution", ":attention"]
    replace: |
     ```ad-warning
     title: $|$
     ```
  - triggers: [":failure", ":fail", ":missing", ":miss"]
    replace: |
     ```ad-failure
     title: $|$
     ```
  - triggers: [":danger", ":error"]
    replace: |
     ```ad-danger
     title: $|$
     ```
  - trigger : ":bug"
    replace: |
     ```ad-bug
     title: $|$
     ```
  - triggers: [":ex", ":example"]
    replace: |
     ```ad-example
     title:$|$
     ```
  - triggers: [":cite", ":citation", ":>"]
    replace: |
     ```ad-quote
     title: $|$
     ```

#Content

  - triggers: ["/note", "/seealso"]
    replace: |
     ```ad-note
     $|$
     ```
  - triggers: ["/tldr","/summ", "/summary"]
    replace: |
     ```ad-abstract
     $|$
     ```
  - triggers: ["/info","/todo"]
    replace: |
     ```ad-info
     $|$
     ```
  - triggers: ["/tip", "/hint", "/important", "/def"]
    replace: |
     ```ad-tip
     $|$
     ```
  - triggers: ["/success", "/suc", "/check","/done"]
    replace: |
     ```ad-success
     $|$
     ```
  - triggers: ["/q","/question", "/help","/faq"]
    replace: |
     ```ad-question
     $|$
     ```
  - triggers: ["/warn", "/warning", "/caution", "/attention"]
    replace: |
     ```ad-warning
     $|$
     ```
  - triggers: ["/failure", "/fail", "/missing", "/miss"]
    replace: |
     ```ad-failure
     $|$
     ```
  - triggers: ["/danger", "/error"]
    replace: |
     ```ad-danger
     $|$
     ```
  - trigger : "/bug"
    replace: |
     ```ad-bug
     $|$
     ```
  - triggers: ["/ex", "/example"]
    replace: |
     ```ad-example
     $|$
     ```
  - triggers: ["/cite", "/>", "/citation"]
    replace: |
     ```ad-quote
     $|$
     ```